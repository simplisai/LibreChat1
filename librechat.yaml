version: 1.2.1
cache: true

interface:
  customWelcome: "Bem-vindo ao Simplis.ai - Sua plataforma de IA conversacional"
  
registration:
  allowedDomains: []

# Configuração do endpoint LiteLLM
endpoints:
  custom:
    - name: "Simplis AI Models"
      apiKey: "simplis-litellm-master-key-123"
      baseURL: "http://litellm:8000"
      models:
        default: ["gpt-4o-mini", "gpt-4o", "gpt-3.5-turbo"]
        fetch: true
      titleConvo: true
      titleModel: "gpt-3.5-turbo"
      summarize: false
      summaryModel: "gpt-3.5-turbo"
      forcePrompt: false
      modelDisplayLabel: "Simplis AI"
      iconURL: "/favicon-32x32.png"

    # Endpoint OpenAI direto (backup)
    - name: "OpenAI Direct"
      apiKey: "${OPENAI_API_KEY}"
      baseURL: "https://api.openai.com/v1"
      models:
        default: ["gpt-4o", "gpt-4o-mini", "gpt-3.5-turbo"]
        fetch: false
      titleConvo: true
      titleModel: "gpt-3.5-turbo"
      modelDisplayLabel: "OpenAI"

# Rate limits
rateLimits:
  fileUploads:
    ipMax: 100
    ipWindowInMinutes: 60
    userMax: 50
    userWindowInMinutes: 60

# File configuration
fileConfig:
  serverFileSizeLimit: 100
  avatarSizeLimit: 2
